# OKF Workbench 0.4.0 release record

Status: Final exact-artifact performance and CI pass; completing hosted qualification before publication.

## Scope

Node 24/CommonJS Extension Host output, `@types/node` 24.13.3, VS Code 1.123 minimum,
and VSCodium 1.126.04524 qualification, as accepted in [ADR 0012](../decisions/0012-node24-extension-host.md).
Development dependency updates include undici, fast-uri, js-yaml, postcss, and nanoid.
The format semantics, local-first behavior, Wasm ABI, and native CLI features are unchanged.

Windows watcher events now compare canonically serialized VS Code URIs so `/C:/` roots
accept `/c:/` events while retaining containment checks. Valid UTF-8 documents within
the source-size limit cross the Wasm JSON ABI as strings. Malformed and oversized byte
inputs retain their original diagnostics. Regression coverage includes BOM, CRLF,
Unicode, invalid UTF-8, source-size boundaries, and all watcher event types.

Release compilation uses optimization level 3 with existing LTO and one codegen unit.
Provider operations use fixed batches of sixteen, retaining the per-document/aggregate
byte limits, parent-generation checks, and draining of canceled physical calls. The
headed runner supports Windows process counters, CLI invocation, and signaled process exit.
Performance thresholds and the 250 ms watcher debounce are unchanged.

## Qualification

The final packaged-input revision is `06287122d3ceaa028875643276fdaca8e92aef7b`.
[CI 33965910248](https://github.com/koizumikento/okf-workbench/actions/runs/33965910248)
passes all four jobs, including minimum/current VS Code integration.
[Compatibility 33965909336](https://github.com/koizumikento/okf-workbench/actions/runs/33965909336)
tracks all seven editor/OS lifecycle lanes using the published 0.3.0 universal VSIX
(SHA-256 `d96aa19cc3ada5d4e34618d41bedc4195efa368e2b51b8ff8977984e815caf57`).
[Package smoke 33965908284](https://github.com/koizumikento/okf-workbench/actions/runs/33965908284)
tracks the browser boundary, four target packages, and aggregate package-set consistency.
Both hosted workflows must complete successfully before tagging. Subsequent evidence
and release-record documentation changes do not change packaged inputs.

The hosted universal candidate is **1,459,446 bytes**, SHA-256
`ac11074af65f02ddeb1ff3bbf0f58d12a2b1327281a13d72fa21b0d3d5b5dcf8`.
It is byte-for-byte identical to the deterministic package made from the measured runtime,
including all 13 extension payload files. The canonical Wasm SHA-256 is
`000f1fd0a721285e31ec4ec6673b4717892a468a77116225dd656eccbb37dc2f`.
The [CI build receipt](../evidence/performance/vscode-1.129.1-0.4.0-build-metadata.json)
binds Rust inputs `80916738fa3be41c86ade827e392ebf484282d9f8438f325c51ae0fd96c29a92`.

The genuine [Windows performance report](../evidence/performance/vscode-1.129.1-0.4.0.md)
and [raw schema-v3 capture](../evidence/performance/vscode-1.129.1-0.4.0.json) pass the strict
current-input evaluator: QR-002 p95 **851 ms across 20 samples**, QR-003 selects `d3`,
and remote HTTP(S)/WS and other-scheme request counts are zero. This capture is specific
to Windows 11, Ryzen 9 9900X, RTX 5070, and VS Code 1.129.1; it is not a cross-machine guarantee.
See [performance evidence](../performance-evidence.md) for all hashes and reproduction.

Earlier captures remain historical, including 1,252 ms with mismatched local Wasm,
961 ms on a676122, and 1,190 ms after the byte-limit fix on f793844. None is reused to
qualify the final bytes. An exploratory JSON response refactor had no measured speed
benefit and was not retained.

Local checks pass: 108 Wasm/provider tests, 52 Rust core/Wasm tests, workspace clippy,
type checking, focused lint, and formatting. Earlier immutable-evidence/materialization
tests also passed. Local Windows could not create one symbolic-link fixture (`EPERM`)
or load Microsoft.PowerShell.Security for a CLI ACL test. Hosted Linux/Windows gates
cover these checks; no test or safety threshold was weakened to conceal workstation limits.

## Publication

The maintainer authorized publication on 2026-09-05. A matching version tag on reviewed
main starts the workflow, as specified by [ADR 0006](../decisions/0006-publish-open-vsx-from-version-tags.md).
Neither the decision nor the workflow requires a cryptographic tag signature. Historical
signed releases do not add that prerequisite. No 0.4.0 tag has yet been published.
Follow [the release checklist](../release-checklist.md) for publication and verification.
