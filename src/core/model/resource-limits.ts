/** Deterministic semantic-output limits applied after bounded provider reads. */
export const OKF_SEMANTIC_LIMITS = Object.freeze({
  maxProviderPathCodeUnits: 4_096,
  maxProviderPathBytes: 4_096,
  maxProviderPathSegments: 64,
  // A 4 KiB provider path may expand to roughly 12 KiB when percent-encoded
  // into a serialized URI, in addition to the scheme, authority, and root.
  maxSourceUriCodeUnits: 16 * 1024,
  maxSourceUriBytes: 16 * 1024,
  maxContentHashCodeUnits: 256,
  maxFailureDetailCodeUnits: 4_096,
  maxFrontmatterSourceBytes: 64 * 1024,
  maxFrontmatterSourceCodeUnits: 64 * 1024,
  maxFrontmatterLines: 4_000,
  maxFrontmatterStructuralTokens: 8_000,
  maxBundleFrontmatterSourceCodeUnits: 8 * 1024 * 1024,
  maxBundleFrontmatterStructuralTokens: 256_000,
  maxFrontmatterNestingDepth: 64,
  maxFrontmatterIndentColumns: 128,
  maxFrontmatterOutputUnits: 128 * 1024,
  maxBundleFrontmatterOutputUnits: 16 * 1024 * 1024,
  maxSemanticDocumentBytes: 320 * 1024 + 16,
  maxSemanticDocumentCodeUnits: 320 * 1024 + 16,
  maxSemanticDocumentLines: 24_002,
  maxMarkdownBodyBytes: 256 * 1024,
  maxMarkdownBodyCodeUnits: 256 * 1024,
  maxMarkdownLines: 20_000,
  maxMarkdownSyntaxCandidates: 20_000,
  maxBundleMarkdownBodyCodeUnits: 8 * 1024 * 1024,
  maxBundleMarkdownLines: 100_000,
  maxBundleMarkdownSyntaxCandidates: 80_000,
  maxBundleMarkdownLinkCandidates: 20_000,
  maxBundleMarkdownAttentionWorkUnits: 32 * 1024 * 1024,
  maxBundleMarkdownContainerWorkUnits: 262_144,
  maxBundleMarkdownLabelEndWorkUnits: 32 * 1024 * 1024,
  maxMarkdownContainerNestingDepth: 64,
  maxMarkdownContainerWorkUnitsPerDocument: 65_536,
  maxMarkdownMediaNestingDepth: 64,
  maxMarkdownAttentionRunsPerDocument: 1_024,
  maxMarkdownAttentionMarkerCodeUnitsPerDocument: 1_024,
  maxMarkdownAttentionWorkUnitsPerDocument: 8 * 1024 * 1024,
  maxMarkdownLabelEndWorkUnitsPerDocument: 8 * 1024 * 1024,
  maxMarkdownLinksPerDocument: 5_000,
  maxMarkdownDefinitionsPerDocument: 5_000,
  maxLinkTargetBytes: 2_048,
  maxLinkTargetCodeUnits: 2_048,
  maxLinkLabelBytes: 512,
  maxLinkLabelCodeUnits: 512,
  maxLinkTextUnitsPerDocument: 1024 * 1024,
  maxBundleLinkTextUnits: 4 * 1024 * 1024,
  maxTypeCodeUnits: 256,
  maxTypeBytes: 256,
  maxTagCodeUnits: 256,
  maxTagBytes: 256,
  maxTagsPerConcept: 128,
  maxBundleTagAssignments: 20_000,
  maxUniqueGraphTypes: 512,
  maxUniqueGraphTags: 4_096,
  maxTitleCodeUnits: 4_096,
  maxDescriptionCodeUnits: 16_384,
  maxResourceCodeUnits: 4_096,
  maxTimestampCodeUnits: 256,
  maxRuntimeDocuments: 2_000,
  maxBundleLinks: 10_000,
  maxGraphNodes: 2_000,
  maxGraphEdges: 10_000,
  maxGraphIdentityBytes: 4 * 1024 * 1024,
  maxGraphPayloadBytes: 16 * 1024 * 1024,
  maxCompactGraphEdgeIdCodeUnits: 64,
  maxFindings: 20_000,
  maxDuplicateResourcePeerIds: 8,
});
