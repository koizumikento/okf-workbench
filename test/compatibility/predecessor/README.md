# Compatibility predecessor

This is a test-only `straydog.okf-workbench@0.0.0` package. The compatibility
harness installs it before the release candidate to exercise a real VSIX
upgrade without claiming that this fixture was a published release.
